-- Optional default dropdown values for a new organization.
-- Replace the placeholder organization id before running, or seed through the admin app later.

-- Example:
-- insert into public.organizations (id, name)
-- values ('00000000-0000-0000-0000-000000000001', 'Your Company')
-- on conflict do nothing;

-- Then replace :organization_id below with that id if running manually.
